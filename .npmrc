registry=https://registry.npmjs.org/

# React Native (a peer dependency of @mitumba/pay-native) pulls its own React
# peer range that can conflict with dev React versions across the workspace.
# Peers are provided by the host app at consumption time, so we relax auto peer
# installation here. This keeps `npm ci` reproducible across the monorepo.
legacy-peer-deps=true
