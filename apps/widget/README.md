# @mitumba/pay-widget-app (private)

Hosts `@mitumba/pay-react` as a standalone widget deployed to
`pay.mitumba.africa/widget`. Private — never published to npm (in the Changesets
`ignore` list). Framework (Vite/Next) to be added in a follow-up.
